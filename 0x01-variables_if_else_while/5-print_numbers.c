#include <stdio.h>

/**
 * main - print all strings from 0 to 9
 * Returns: Always 0
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d", num);

	pintf("\n");

	return (0);
}

